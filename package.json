{
  "name": "hono-base-monorepo",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:packages": "pnpm --filter @repo/db build && pnpm --filter @repo/auth build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.30.2",
  "devDependencies": {
    "turbo": "^2.8.10",
    "typescript": "^5.9.3"
  }
}
